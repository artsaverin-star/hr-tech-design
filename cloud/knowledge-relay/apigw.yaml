openapi: 3.0.0
info:
  title: bulochka-knowledge
  version: 1.0.0
paths:
  /knowledge:
    x-yc-apigateway-any-method:
      summary: Read (GET) or append (POST) the shared knowledge base
      operationId: knowledge
      x-yc-apigateway-integration:
        type: cloud_functions
        # заменить на id функции и id сервис-аккаунта после их создания:
        function_id: REPLACE_FUNCTION_ID
        service_account_id: REPLACE_SA_ID
